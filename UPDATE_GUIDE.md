# 🚀 Инструкция по использованию системы обновлений

## Что это такое?

Автоматическая система обновления, которая:
- ✅ Проверяет новые версии каждые 5 минут
- ✅ Показывает уведомление в верхней части экрана
- ✅ Позволяет обновить проект одним кликом
- ✅ Берет информацию об обновлениях из GitHub
- ✅ Загружает changelog из README или Release Notes

---

## Как использовать для разработчика?

### 1️⃣ Когда вы готовы выпустить новую версию

#### Вариант A: Через GitHub Releases (рекомендуется)

**На локальной машине:**
```bash
cd c:\Users\yuliitezary\Desktop\telegram-bot-arenda-ps4-ps5-main

# Делаете свои изменения
git add .
git commit -m "Описание изменений"

# Создаете тег версии (семантическое версионирование: major.minor.patch)
git tag -a v1.1.0 -m "Version 1.1.0 - New features and bugfixes"

# Загружаете на GitHub
git push origin main
git push origin v1.1.0
```

**На GitHub:**
- Переходите в "Releases" → "Create a new release"
- Выбираете тег `v1.1.0`
- В поле "Release notes" пишете что изменилось:
  ```
  ## Новое
  - Добавлена функция X
  - Улучшена функция Y

  ## Исправлено
  - Исправлена ошибка Z
  
  ## Удалено
  - Удалена устаревшая функция W
  ```
- Нажимаете "Publish release"

#### Вариант B: Через README.md

**Если нет релизов, система ищет версию в README:**

```markdown
# PlayStation Аренда

**Текущая версия: 1.1.0**

## 📋 История обновлений

### v1.1.0 (2025-01-15)
- ✨ Добавлена новая система рейтингов
- 🔧 Исправлены ошибки в календаре
- 🎨 Улучшен дизайн админ-панели
- ⚡ Ускорена загрузка данных

### v1.0.0 (2025-01-01)
- 🎉 Первый релиз
```

Затем:
```bash
git add README.md
git commit -m "Update version to 1.1.0"
git push origin main
```

---

## Как работает для пользователей?

### 📲 Администраторы видят:

1. **При загрузке страницы / через 5 минут:**
   - В верхней части экрана появляется фиолетовое уведомление
   - "Доступно обновление 1.1.0"
   - Показывает текущую версию и новую версию

2. **В уведомлении три элемента:**
   - 📋 Кнопка "Что изменилось?" - показывает changelog
   - ⭐ Кнопка "Обновить сейчас" - загружает обновление
   - 🕐 Кнопка "Напомнить позже" - скрывает уведомление

3. **При нажатии "Обновить сейчас":**
   ```
   - Требует подтверждение
   - Загружает обновления с GitHub (git pull)
   - Обновляет версию в version.json
   - Перезагружает приложение
   ```

### 👥 Обычные пользователи:

- Если пользователь не администратор, кнопка "Обновить сейчас" неактивна
- Уведомление всё равно видно (для информации)
- Может нажать только "Напомнить позже"

---

## Файлы системы

### 1. `update_manager.py` - Основной модуль
```python
# Проверяет обновления в GitHub
# Сравнивает версии
# Выполняет git pull
# Сохраняет информацию о версии
```

### 2. `static/update-notifier.js` - JavaScript
```javascript
// Показывает уведомление в браузере
// Обрабатывает кнопки (обновить, отклонить)
// Периодически проверяет обновления
```

### 3. `version.json` - Информация о версии
```json
{
  "current_version": "1.0.0",
  "github_version": "1.1.0",
  "update_available": true,
  "changelog": "...",
  "last_check": "2025-01-15T12:00:00"
}
```

### 4. `app.py` - API эндпоинты
```python
/api/check-update              # Проверить обновления
/api/get-update-notification   # Получить уведомление
/api/update-application        # Установить обновление
/api/dismiss-update            # Отклонить уведомление
```

### 5. `templates/base.html` - Подключение скрипта
```html
<script src="static/update-notifier.js"></script>
```

---

## Пример: Полный цикл обновления

### Шаг 1️⃣: Разработчик готовит обновление

```bash
# На локальной машине
git add .
git commit -m "v1.2.0: Fixed bugs and added features"
git tag -a v1.2.0 -m "Version 1.2.0"
git push origin main v1.2.0
```

### Шаг 2️⃣: GitHub Release

На GitHub:
- Создайте Release для тага v1.2.0
- Напишите что изменилось
- Опубликуйте

### Шаг 3️⃣: Администратор видит обновление

- При загрузке админ-панели система проверит GitHub
- Обнаружит версию 1.2.0 вместо 1.0.0
- Покажет уведомление в верхней части

### Шаг 4️⃣: Администратор обновляет

- Нажимает "Обновить сейчас"
- Подтверждает в диалоге
- Приложение скачивает обновления и перезагружается

---

## Частые вопросы

### Q: Как проверить текущую версию в коде?

```python
from update_manager import get_update_manager

manager = get_update_manager()
version = manager.get_current_version()
print(f"Версия: {version}")
```

### Q: Как отключить уведомления?

Пользователь нажимает "Напомнить позже" - уведомление скроется.
При следующей проверке обновлений оно появится снова.

### Q: Что если обновление не работает?

1. Проверьте интернет
2. Убедитесь что git установлен
3. Проверьте права доступа на запись
4. Посмотрите логи в консоли

### Q: Как вернуть старую версию?

```bash
git revert <commit-hash>
git push origin main
git tag -a v1.0.1 -m "Hotfix"
git push origin v1.0.1
```

### Q: Система не видит новую версию?

- Убедитесь что тег создан правильно: `git tag -a v1.2.0 -m "..."`
- Загрузили тег на GitHub: `git push origin v1.2.0`
- Прошло время на обновление (система проверяет каждые 5 минут)
- Попробуйте нажать F5 и обновить страницу

---

## Требования

- ✅ Git установлен
- ✅ Доступ в интернет
- ✅ requests библиотека (`pip install requests`)
- ✅ Доступ на запись в папку проекта

---

## Версионирование

Используется **семантическое версионирование** (SemVer):

```
v1.2.3
  ↑ ↑ ↑
  │ │ └─ Patch (исправления): v1.2.3 → v1.2.4
  │ └─── Minor (новые функции): v1.2.0 → v1.3.0
  └───── Major (большие изменения): v1.0.0 → v2.0.0
```

Примеры:
- `v1.0.0` - первый релиз
- `v1.0.1` - исправление ошибки
- `v1.1.0` - новая функция
- `v2.0.0` - полный переход

---

## Примеры использования

### Обновление 1: Добавить функцию

```bash
# Код
echo "# New feature" >> app.py
git add .
git commit -m "Added new feature"

# Версия
git tag -a v1.1.0 -m "Added new feature"
git push origin main v1.1.0

# GitHub Release
# Title: v1.1.0
# Description: Added new feature for user management
```

### Обновление 2: Исправить баг

```bash
# Код
echo "# Bug fix" >> app.py
git add .
git commit -m "Fixed critical bug"

# Версия (patch)
git tag -a v1.0.1 -m "Fixed critical bug"
git push origin main v1.0.1
```

### Обновление 3: Большой рилиз

```bash
# Много изменений
git add .
git commit -m "v2.0.0: Complete redesign"

# Версия (major)
git tag -a v2.0.0 -m "Complete redesign with new features"
git push origin main v2.0.0

# GitHub Release
# Title: v2.0.0 - Complete Redesign
# Description: [подробное описание]
```

---

## Документация

Полная документация: смотри `UPDATE_SYSTEM_README.md`

---

## Поддержка

Если что-то не работает:
1. Проверьте логи в консоли
2. Создайте Issue на GitHub
3. Свяжитесь с разработчиком

---

**Удачных обновлений!** 🚀
