# 📦 Система Обновлений Приложения

## Описание

Полнофункциональная система автоматических обновлений проекта с проверкой версий и уведомлениями.

## Как это работает

### 1. **Проверка обновлений**
- Система автоматически проверяет GitHub репозиторий каждые 5 минут
- При загрузке страницы сразу же проверяет наличие новой версии
- При возврате пользователя на вкладку браузера также проверяет обновления

### 2. **Версионирование**
- Версия хранится в `version.json`
- Система ищет версию в GitHub релизах (tag name)
- Если релизов нет, ищет версию в README.md
- Версии сравниваются в формате `X.Y.Z` (например, `1.0.0`, `1.2.3`)

### 3. **Уведомления**
- При обнаружении новой версии появляется яркое уведомление в верхней части экрана
- Уведомление показывает:
  - Текущую версию
  - Доступную версию
  - Changelog (что изменилось)

### 4. **Обновление**
- Только администраторы могут обновлять приложение
- При нажатии "Обновить сейчас":
  - Выполняется `git pull origin main`
  - Локальная версия обновляется
  - Приложение перезагружается

## Структура проекта

### Файлы обновления:

```
update_manager.py          # Основной модуль управления обновлениями
static/update-notifier.js  # JavaScript для показа уведомлений
version.json              # Файл с информацией о версии
```

### Интеграция в приложение:

**app.py:**
- Инициализация `UpdateManager`
- API эндпоинты:
  - `GET /api/check-update` - проверить обновления
  - `GET /api/get-update-notification` - получить уведомление
  - `POST /api/update-application` - установить обновление
  - `POST /api/dismiss-update` - отклонить уведомление

**templates/base.html:**
- Подключение скрипта `update-notifier.js`
- Передача информации о том, является ли пользователь администратором

## Использование

### Для разработчика

#### 1. Создание новой версии с помощью GitHub Releases:

```bash
# Локально
git tag -a v1.1.0 -m "Description of changes"
git push origin v1.1.0

# На GitHub создайте Release с tag v1.1.0
# Добавьте описание изменений в поле "Release notes"
```

#### 2. Или обновите README.md с версией:

```markdown
# Версия 1.1.0

## Что изменилось
- Исправлена ошибка X
- Добавлена функция Y
```

#### 3. Загрузите на GitHub:

```bash
git add README.md
git commit -m "Update version to 1.1.0"
git push origin main
```

### Для пользователя (администратора)

1. **Получение уведомления:**
   - При наличии новой версии в верхней части экрана появится уведомление
   - Кнопка "Обновить сейчас" активна только для администраторов

2. **Обновление:**
   - Нажмите "Обновить сейчас"
   - Подтвердите в диалоге
   - Дождитесь завершения
   - Приложение автоматически перезагружается

3. **Отложить обновление:**
   - Нажмите "Напомнить позже"
   - Уведомление исчезнет
   - При следующей проверке версии оно появится снова

## API Эндпоинты

### GET /api/check-update
Проверяет наличие обновлений.

**Ответ:**
```json
{
  "success": true,
  "update_available": true,
  "current_version": "1.0.0",
  "github_version": "1.1.0",
  "changelog": "- Исправлена ошибка\n- Добавлена функция",
  "last_check": "2025-01-01T12:00:00"
}
```

### GET /api/get-update-notification
Получает информацию об обновлении для админ-панели.

**Ответ:**
```json
{
  "success": true,
  "notification": {
    "has_update": true,
    "current": "1.0.0",
    "available": "1.1.0",
    "changelog": "...",
    "last_check": "2025-01-01T12:00:00"
  }
}
```

### POST /api/update-application
Загружает и устанавливает обновление (только для администраторов).

**Ответ (успех):**
```json
{
  "success": true,
  "message": "Обновление успешно установлено",
  "new_version": "1.1.0",
  "restart_required": true
}
```

### POST /api/dismiss-update
Отклоняет уведомление об обновлении.

**Ответ:**
```json
{
  "success": true,
  "message": "Уведомление отклонено"
}
```

## Файл version.json

Хранит информацию о версии приложения:

```json
{
  "current_version": "1.0.0",
  "last_check": "2025-01-01T12:00:00",
  "update_available": false,
  "github_version": null,
  "changelog": ""
}
```

## Требования

- `requests>=2.31.0` - для работы с GitHub API
- `git` - для загрузки обновлений (git pull)
- Доступ в интернет для проверки GitHub

## Установка

1. Обновите `requirements.txt`:
```bash
pip install -r requirements.txt
```

2. Запустите приложение - система обновлений загрузится автоматически

## Примеры использования

### Пример 1: Создание новой версии

```bash
# Локально делаем изменения
git add .
git commit -m "v1.1.0: Added new features"

# Создаем тег
git tag -a v1.1.0 -m "Version 1.1.0 - New features"

# Загружаем на GitHub
git push origin main
git push origin v1.1.0

# На GitHub создайте Release
```

### Пример 2: Обновление через README

```markdown
# PlayStation Аренда - Telegram Bot

**Версия: 1.1.0**

## Последние обновления
- ✨ Добавлена новая система рейтингов
- 🔧 Исправлены ошибки в календаре
- 🎨 Улучшен дизайн админ-панели
```

## Отладка

### Проверить текущую версию

```python
from update_manager import get_update_manager

manager = get_update_manager()
print(f"Текущая версия: {manager.get_current_version()}")
```

### Сделать проверку обновлений вручную

```python
from update_manager import get_update_manager

manager = get_update_manager()
result = manager.check_for_updates()
print(result)
```

### Просмотреть логи

Логи обновлений выводятся в консоль приложения.

## Безопасность

- Обновление доступно только администраторам
- Используется официальный GitHub API
- Версии сравниваются корректно
- Git pull выполняется безопасно

## Ограничения

- Требуется интернет соединение для проверки обновлений
- GitHub API имеет лимит на количество запросов (60 запросов в час без аутентификации)
- Обновление работает только если репозиторий доступен через git

## Будущие улучшения

- [ ] Уведомления в Telegram боте об обновлениях
- [ ] Автоматический откат при ошибке обновления
- [ ] История обновлений
- [ ] Планирование обновлений на определенное время
- [ ] Отправка уведомлений всем администраторам

## Поддержка

Если возникли проблемы с обновлениями:

1. Проверьте интернет соединение
2. Убедитесь, что у вас есть доступ на запись в директорию проекта
3. Проверьте логи в консоли
4. Создайте issue на GitHub
